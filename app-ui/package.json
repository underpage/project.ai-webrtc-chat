{
  "name": "app-ui",
  "version": "1.0.0",
  "description": "WebRTC 화상 회의 플랫폼의 사용자 인터페이스(UI) 입니다",
  "license": "ISC",
  "type": "module",
  "main": "vite.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && rm -rf ../app-server/public/* && cp -r dist/* ../app-server/public/"
  },
  "dependencies": {
    "dayjs": "^1.11.19",
    "global": "^4.4.0",
    "nanostores": "^1.1.0",
    "normalize.css": "^8.0.1",
    "webrtc-adapter": "^9.0.3"
  },
  "devDependencies": {
    "glob": "^13.0.0"
  }
}
